[pytest]
testpaths = tests
# Prepend the backend directory so `app` resolves to this package. Without it,
# an unrelated app.py higher up the filesystem can shadow the whole package.
pythonpath = .
addopts = -q --strict-markers
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
